from setuptools import setup

setup(name='MUSE',
      version='0.1',
      description='Self-interpretable graph learning framework',
      license='MIT',
      package_dir={'': '2_code'},
      packages=['gpl'],
      zip_safe=False)
